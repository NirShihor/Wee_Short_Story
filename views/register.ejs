  <div class="logoContainer">
    <img class="logo" src="/images/WSS logo.png" alt="logo">
  </div>
  <div class='greeting'>
    <div class="hiddenButton">
      <a href='/' class='homeButton''>Home Page</a>
  </div>
</div>
  <br>

<div class="outerContainer">
    <div class="card">
      <h1 class="heading">
        <i class="fas fa-user-plus"></i> Register</h1>
      <!-- Brings in the messages for the user from messages.ejs -->
      <%- include ('./partials/messages') %>
      <div class="outerForm">
        <form action="/users/register" method="POST">
          <div class="form-group">
            <label for="name"></label>
            <input type="name" id="name" name="name" class="form-control" autofocus placeholder="Enter Name"
              value="<%= typeof name != 'undefined' ? name : '' %>" />
          </div>
          <div class="form-group">
            <label for="email"></label>
            <input type="email" id="email" name="email" class="form-control" placeholder="Enter Email"
              value="<%= typeof email != 'undefined' ? email : '' %>" />
          </div>
          <div class="form-group">
            <label for="password"></label>
            <input type="password" id="password" name="password" class="form-control" placeholder="Create Password"
              value="<%= typeof password != 'undefined' ? password : '' %>" />
          </div>
          <div class="form-group">
            <label for="password2"></label>
            <input type="password" id="password2" name="password2" class="form-control" placeholder="Confirm Password"
              value="<%= typeof password2 != 'undefined' ? password2 : '' %>" />
          </div>
          <div class="buttonFrame">
            <button class="formButton" type="submit" class="btn btn-primary btn-block">
              Register
            </button>
          </div>
        </form>
      </div>
      <div class="register">
        <p class="registerForgot">Already Have An Account? <a class="login" href="/users/login">Login</a></p>
      </div>
    </div>
  </div>
</div>


<style>
            .heading { text-align: center; margin-top:
        3em; } .lead { margin-top: 0.5em !important; } .card { margin: auto; width: 30%; } .outerForm { border: 3px
        solid green !important; padding: 0.6em 2em 2em 2em; border-radius: 0.5em; } .form { width: 100%; text-align:
        left; } input { width: 100%; height: 2.5em; padding-left: 1em; margin-top: 1em; margin-bottom: 1em; font-size:
        1em; } label { font-size: 1.2em; } .formButton { height: 2.5em; width: 6em; font-size: 0.8em; cursor: pointer;
        border-radius: 0.6em; } .register { padding-left: 2.5em; font-size: 1.2em; color: #375c1a; font-weight: 900;
        font-size: 1.3em; } .buttonFrame { text-align: center; } .login { text-decoration: none; color: #4d4d7c; }
        @media (max-width: 800px) { .heading { margin-top: 2.5em; } } @media (max-width: 800px) { .card { width: 24em; }
        } .outerContainer { margin: auto; } @media (max-width: 800px) { .outerContainer { margin-top: 15rem; } }
         .logo {
        width: 12rem; left: 5rem; position: absolute; top: 2em; } @media (max-width: 800px) { .logo { margin-left: -5em;
        } } .hiddenButton { margin-top: 2em; } @media (max-width: 800px) { .hiddenButton { margin-right: -4em;
        margin-top: 4em;} } </style>